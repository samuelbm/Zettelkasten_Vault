---
type: Fleeting
id: <% tp.date.now("YYYYMMDDHHmmss") %>
tags:
  - fleeting
---
# Thoughts
* 

# References / Sources
* 

<%*
const dateStr = tp.date.now("YYYY-MM-DD-MM-SS");  

// Folder to move note into
const folderPath = "01 - Fleeting Notes";

// Prompt for note title
const noteName = "Fleeting Note " + dateStr;

// Move the note
await tp.file.move(`${folderPath}/${noteName}`);
%>
